"use strict";

const workStatusEnum = Object.freeze({
  FREELANCER: "FREELANCER",
  EMPLPOYED: "EMPLOYED",
  UNEMPLOYED: "UNEMPLOYED",
});

const developerTypeEnum = Object.freeze({
  FULLSTACK: "FULLSTACK",
  FRONTEND: "FRONTEND",
  BACKEND: "BACKEND",
});
